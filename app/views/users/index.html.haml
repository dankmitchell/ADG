- content_for :page_title, "Users - ADG Exercise"

%main{id: "content", role: "main"}
  .grid-row
    .column-full
      %h1.heading-large Users
      %table.table
        %thead
          %tr
            %th Email
            %th Signed up
        %tbody
          - @users.each do |user|
            %tr= render user
